# Postgres (setup:db e serviços locais)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/reserveHub
CORE_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/reserveHub?schema=core
IAM_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/reserveHub?schema=iam

# JWT (mesmo valor em core + iam)
JWT_SECRET=reservehub-dev-secret-change-me

# Seeds
SEED_DEMO_PASSWORD=ReserveHub1!

# Integração
RUN_INTEGRATION_DB=0
